{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "pnpm --filter @workspace/haq-enterprises run dev",
    "build": "pnpm --filter @workspace/haq-enterprises run build",
    "typecheck": "pnpm --filter @workspace/haq-enterprises run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}